##  Be sure to run `pod spec lint MyFFmpeg.podspec' to ensure this is a#  valid spec and to remove all comments including this before submitting the spec.##  To learn more about Podspec attributes see https://guides.cocoapods.org/syntax/podspec.html#  To see working Podspecs in the CocoaPods repo see https://github.com/CocoaPods/Specs/#Pod::Spec.new do |s|  s.name             = 'MyFFmpeg'  s.version          = '1.0.0'  s.summary          = 'A simple wrapper around FFmpeg for iOS'  s.description      = 'This library wraps FFmpeg for use in iOS apps.'  s.homepage         = 'https://github.com/pethanihiten/MyFFmpeg'  s.license          = { :type => 'MIT', :file => 'LICENSE' }  s.author           = { 'Hiten Pethani' => 'pethanihiten@gmail.com' }  s.source           = { :git => 'https://github.com/pethanihiten/MyFFmpeg.git', :tag => 'v1.0.0' }  s.platform         = :ios, '10.0'  # Vendored framework(s) location  s.vendored_frameworks = 'Frameworks/*.framework'  # Optional: Specify source files if you have any additional sources  s.source_files     = 'Sources/**/*.{h,m,swift}'  # Link against necessary frameworks (if needed)  s.frameworks       = 'UIKit', 'AVFoundation'  s.requires_arc     = trueend